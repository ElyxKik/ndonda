rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // RÈGLES DE DÉVELOPPEMENT - À REMPLACER EN PRODUCTION
    // Permet la lecture et l'écriture sans authentification
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
